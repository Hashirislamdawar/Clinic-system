fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
sqlalchemy>=2.0,<3.0
pydantic[email]>=2.7,<3.0
pydantic-settings>=2.0
alembic>=1.13
python-dotenv>=1.0
# PostgreSQL driver (psycopg 3). Only needed when DATABASE_URL points at
# PostgreSQL; the default SQLite setup does not require it.
psycopg[binary]>=3.1

# Dev / test
pytest>=8.0
httpx>=0.27
